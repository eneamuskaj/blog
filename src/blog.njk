---
title: Blog
layout: layout.njk
---

<h1>Blog Posts</h1>

<ul>
  {% for post in collections.posts %}
    <li>
      <a href="{{ post.url }}">{{ post.data.title }}</a>
      <p>{{ post.date | readableDate }}</p>
    </li>
  {% endfor %}
</ul>
